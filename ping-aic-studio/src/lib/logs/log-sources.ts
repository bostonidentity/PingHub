/** Log sources the archive supports (AM + IDM). Shared by the API + the UI. */
export const DEFAULT_LOG_SOURCES = [
    "am-authentication",
    "am-access",
    "am-core",
    "idm-access",
    "idm-activity",
    "idm-authentication",
];
